# Config

Folder ini berisi contoh konfigurasi non-secret untuk menjelaskan environment yang dibutuhkan.

Actual secrets harus disimpan di environment/dashboard/server config, bukan repo.

See also: `.mcp.json.example` and `TOOLS.md`.
