# 2026-04-29

Initial repo skeleton created for SOREA UMKM Agent.

Decisions:
- Repo must include OpenClaw root files.
- `skills/` and `agents/` are the highest-value/evaluated folders.
- Scope includes `/ig_*`, `/keu_*`, `/katalog_*`, `/produk_*`, `/pesan`, `/bayar`, and inbox/CS.
